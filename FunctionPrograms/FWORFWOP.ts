function calculating() : void // Void is a retunr type without value and without return types
{
    let a : number = 32;
    let b : number = 31;
    console.log("The sum of a and b is " + (a+b));
}

calculating();    // We are calling the function with the function name