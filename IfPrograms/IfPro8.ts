let h : boolean  = false    // = .--> assigning operator

if (h == false)        // == ---> here comparing one false to another false (false == false), so the problem will run
{
    console.log("Roses are red, sky is blue")
}
